# Plano de implementação — largura desktop

1. Confirmar o contêiner global que limita a interface web a 1024 px.
2. Manter o limite de 1024 px apenas para viewports de tablet e liberar o `#root` em desktop.
3. Validar TypeScript, build de produção e renderização desktop sem margens externas.
4. Versionar e enviar somente os arquivos relacionados a esta correção.

