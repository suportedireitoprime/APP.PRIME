import React from 'react';

/**
 * TopRouteProgress
 * Desativado conforme solicitação do usuário: não exibir a linha de carregamento
 * no topo ao navegar entre as páginas.
 */
export const TopRouteProgress: React.FC = () => {
  return null;
};

export default TopRouteProgress;

