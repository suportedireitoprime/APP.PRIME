name: Blog Auto Generation Cron

on:
  schedule:
    # Run every 15 minutes to check if there are scheduled posts
    - cron: '*/15 * * * *'
  workflow_dispatch:

jobs:
  run-blog-runner:
    runs-on: ubuntu-latest
    steps:
      - name: Invoke blog-edicao-runner Edge Function
        run: |
          # Use the public anon key to invoke the edge function securely
          curl -X POST "https://dnjrgpldcwcpoywamorr.supabase.co/functions/v1/blog-edicao-runner" \
            -H "Authorization: Bearer eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJpc3MiOiJzdXBhYmFzZSIsInJlZiI6ImRuanJncGxkY3djcG95d2Ftb3JyIiwicm9sZSI6ImFub24iLCJpYXQiOjE3ODI2ODYxMzMsImV4cCI6MjA5ODI2MjEzM30.GuZuUn1ITbjsTYi_SjL-eFSCxdxxs3rUASArbMf62O0" \
            -H "Content-Type: application/json" \
            -d '{}'
